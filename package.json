{
  "name": "project-references-demo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm --stream -r run build",
    "clean": "pnpm -r run clean",
    "uninstall": "rm -rf node_modules && pnpm -r run uninstall"
  }
}
